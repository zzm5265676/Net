conda create -n CIDNet510 python=3.10 -y
conda activate CIDNet510

python -m pip install --upgrade pip setuptools wheel

pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128

pip install arch==5.3.1 image_quality==1.2.7 lpips==0.1.4 matplotlib==3.8.4 numpy==1.26.4 opencv_contrib_python==4.7.0.72 opencv-python==4.7.0.72 Pillow==9.5.0 scipy==1.11.4 thop==0.1.1.post2209072238 tqdm==4.65.0 einops==0.6.1 gradio==3.34.0 gradio_client==0.2.6 fastapi==0.95.2 starlette==0.27.0 anyio==3.7.1 pydantic==1.10.13 uvicorn==0.22.0 Jinja2==3.1.2 huggingface_hub==0.16.4 safetensors==0.4.3

python -c "import torch; print('torch:', torch.__version__); print('cuda:', torch.cuda.is_available()); print('torch cuda:', torch.version.cuda); print('gpu:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'No CUDA'); print('arch list:', torch.cuda.get_arch_list())"