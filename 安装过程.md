conda create -n hvi python=3.10 -y
conda activate hvi
conda install pytorch==1.13.1 torchvision==0.14.1 torchaudio==0.13.1 pytorch-cuda=11.7 -c pytorch -c nvidia
pip install numpy==1.22.4 opencv-python==4.7.0.72
pip install arch==5.3.1 image_quality==1.2.7 lpips==0.1.4 matplotlib==3.5.3 scipy==1.7.3 thop tqdm==4.65.0 einops==0.6.1 gradio huggingface_hub safetensors


python -c "import torch; import cv2; import numpy; print('CUDA:', torch.cuda.is_available())"


python train.py --dataset lol_v1




# 1. 退出坏环境
conda deactivate

# 2. 删除报错的 hvi 环境
conda remove -n hvi --all -y

# 3. 重建干净环境（python3.10 最稳）
conda create -n hvi python=3.10 -y
conda activate hvi

# 4. 升级 pip
python -m pip install --upgrade pip setuptools wheel

# ==========================
# 核心：安装 【CUDA 12.1 版 torch】
# 兼容你的 CUDA12.3 + 4090，最稳定！
# ==========================
pip install torch==2.3.0 torchvision==0.18.0 torchaudio==2.3.0 --index-url https://download.pytorch.org/whl/cu121

# 5. 安装你需要的所有依赖（我已全部匹配兼容版本）
pip install arch==5.3.1 \
image_quality==1.2.7 \
lpips==0.1.4 \
matplotlib==3.8.4 \
numpy==1.26.4 \
opencv_contrib_python==4.7.0.72 \
opencv-python==4.7.0.72 \
Pillow==9.5.0 \
scipy==1.11.4 \
thop==0.1.1.post2209072238 \
tqdm==4.65.0 \
einops==0.6.1 \
gradio==3.34.0 \
gradio_client==0.2.6 \
fastapi==0.95.2 \
starlette==0.27.0 \
anyio==3.7.1 \
pydantic==1.10.13 \
uvicorn==0.22.0 \
Jinja2==3.1.2 \
huggingface_hub==0.16.4 \
safetensors==0.4.3

# 6. 最终测试（必过！）
python -c "
import torch
import cv2
import numpy
print('CUDA 可用:', torch.cuda.is_available())
print('Torch 版本:', torch.__version__)
print('GPU:', torch.cuda.get_device_name(0))
"